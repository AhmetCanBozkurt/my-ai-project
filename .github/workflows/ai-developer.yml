name: AI Developer Agent

on:
  workflow_dispatch:  # Manuel tetikleme
  schedule:
    # Her gün saat 00:00 UTC'de çalışır
    - cron: '0 0 * * *'
  push:
    branches:
      - main
      - master

jobs:
  ai-developer:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Run AI Agent
        id: ai-agent
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: node scripts/ai-agent.js

      - name: Get Branch Name from AI Agent
        id: branch-name
        run: |
          BRANCH_NAME=$(node -e "const fs=require('fs'); const crypto=require('crypto'); const task=fs.readFileSync('tasks/active-task.md','utf8'); const hash=crypto.createHash('md5').update(task).digest('hex').substring(0,8); console.log('ai-agent/task-'+hash);")
          echo "branch=$BRANCH_NAME" >> $GITHUB_OUTPUT
          echo "🌿 PR Branch: $BRANCH_NAME"

      - name: Create Pull Request
        if: success()
        uses: peter-evans/create-pull-request@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: '🤖 AI Agent: Otomatik kod değişiklikleri'
          title: '🤖 AI Agent: Otomatik Güncellemeler'
          body: |
            Bu PR, AI Agent tarafından otomatik olarak oluşturulmuştur.
            
            **Görev:** `tasks/active-task.md` dosyasındaki talimatlara göre yapılan değişiklikler.
            
            Lütfen değişiklikleri gözden geçirin ve onaylayın.
            
            **Not:** Her görev için ayrı PR açılır. Görev değiştiğinde yeni bir PR oluşturulur.
          branch: ${{ steps.branch-name.outputs.branch }}
          delete-branch: false
          author: AI Developer Agent <ai-agent@github.com>
          committer: AI Developer Agent <ai-agent@github.com>

